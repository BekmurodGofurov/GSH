# Shared test tooling for every Python service.
#
# Kept out of each service's requirements.txt on purpose: those files are what
# the Dockerfiles install, and production images should not ship a test runner.
# CI installs "<service>/requirements.txt" plus this file.
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1
