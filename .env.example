# ==============================================================================
# Game Server Health (GSH) Monitor - Environment Configuration Example
# ==============================================================================

# Database Configuration (PostgreSQL / TimescaleDB)
# Inside Docker (on docker compose up) — alerting-service uses this URL:
POSTGRES_PASSWORD=your_secure_password
DB_URL=postgresql://postgres:${POSTGRES_PASSWORD}@timescaledb:5432/game_monitor
# Local host (when testing outside docker):
# DB_URL=postgresql://postgres:${POSTGRES_PASSWORD}@localhost:5433/game_monitor

# Redis Configuration (Redis Streams & Queue)
# Local host access:
REDIS_URL=redis://localhost:6379
# Docker internal network access:
# REDIS_URL=redis://redis:6379

# Gateway API Configuration
GATEWAY_PORT=8000
INGESTION_PORT=8001
ANOMALY_ML_PORT=8002

# Client Dashboard (for client/.env)
VITE_API_URL=http://<api_host>:8000

# ==============================================================================
# Alerting Service — Telegram Bot
# ==============================================================================
# 1. Create a new bot from @BotFather and get the token:
TELEGRAM_BOT_TOKEN=your_telegram_bot_api
# 2. To find out the group chat ID:
#    - Add the bot to the group
#    - Go to https://api.telegram.org/bot<TOKEN>/getUpdates
#    - Find "chat":{"id": -100xxxxxxxxx} — copy that number here
TELEGRAM_CHAT_ID=-100****

# Daily mode settings:
REPORT_HOUR_UTC=9
REPORT_MINUTE_UTC=0
LOOKBACK_DAYS=1

# true = send a report IMMEDIATELY when the container starts (for testing)
SEND_ON_STARTUP=true
